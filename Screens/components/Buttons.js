import React from "react";
import { View, Text, Button, StyleSheet, TouchableOpacity } from "react-native";

const RegisterButton = () => {
  return <Button title="Register" onPress={() => {}} />;
};
export default RegisterButton;
