import React from "react";
import {
  ScrollView,
  StyleSheet,
  View,
  Image,
  Text,
  TouchableHighlight,
} from "react-native";

const CommentsCreeen = () => {
  return (
    <View style={styles.headerCom}>
      <Text>CreateScreen</Text>
    </View>
  );
};

export default CommentsCreeen;

const styles = StyleSheet.create({
  headerCom: {
    marginTop: 65,
    paddingLeft: 18,
  },
});
